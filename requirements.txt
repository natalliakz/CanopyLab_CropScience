# Direct dependencies, pinned. Connect installs these from Posit Package Manager
# and resolves the rest; `uv.lock` is the exhaustive record for local work.
# The MCP server ships its own, smaller requirements.txt in mcp/.
altair==6.2.2
duckdb==1.5.5
ggsql==0.3.3
great-tables==0.24.0
ipykernel==7.3.0
jupyter-client==8.10.0
matplotlib==3.11.1
nbclient==0.11.0
nbformat==5.11.1
numpy==2.5.2
pandas==3.0.5
polars==1.44.1
pyarrow==25.0.1
pyyaml==6.0.3
scikit-learn==1.9.0
vl-convert-python==1.9.0.post1
