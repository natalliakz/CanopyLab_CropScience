# What Connect installs for the git-backed Streamlit content, from Posit Package
# Manager. Five packages, not the project's seventeen: the dashboard needs the
# app framework, the charts, the dataframe library, the query engine and the
# brand-file parser, and nothing in this bundle needs R, scikit-learn or the
# notebook stack.
#
# This is the one file in connect/streamlit/ that is maintained by hand --
# everything else is written by scripts/build_connect_bundle.py.
streamlit==1.62.0
altair==6.2.2
polars==1.44.1
duckdb==1.5.5
pyyaml==6.0.3
